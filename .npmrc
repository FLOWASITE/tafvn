# This project is primarily managed with bun, which resolves peer dependencies
# leniently. npm is stricter and fails with ERESOLVE on the same tree, so enable
# legacy peer resolution to let `npm install` succeed without extra flags.
# Only affects npm; bun ignores this setting.
legacy-peer-deps=true
